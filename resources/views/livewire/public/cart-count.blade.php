<div>
    @if($count > 0)
        <span class="absolute -top-1 -right-1 flex items-center justify-center w-4 h-4 text-[9px] font-black text-white bg-orange-600 rounded-full border border-white dark:border-zinc-950">
            {{ $count }}
        </span>
    @endif
</div>
